<!doctype html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Gestion de Participantes</title>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.css">
    <link rel="stylesheet" href="/css/animate.css">

</head>

<body>
    <div class="container">

    <% const existeMensaje = typeof mensaje !== "undefined" %>

    <style>
        table, th, td {
            border: 1px solid black;
            border-collapse: collapse;
        }
    </style>

    <% if (existeMensaje) {%>

        <div class="alert alert-primary" role="alert">
            <span class="font-weight-bold">
                MENSAJE
            </span> : <%= mensaje %>
        </div>
    <% }%>

    <h1>Gestion de participantes<br></h1>
     <p>Busque, cree actualice y elimine participantes. Puede buscar participantes por el nombre.</p>

    <form method="get" action="/participante/inicio">

        <div class="row">
            <div class="col-sm-9">
                <div class="form-group">
                    <label for="busqueda">Busqueda</label>
                    <input id="busqueda" type="text" name="busqueda" class="form-control">
                </div>

            </div>
            <div class="col-sm-3">
                <button class=" btn btn-outline-success btn-block" style="margin-top: 31px;">
                    Buscar
                </button>
            </div>
        </div>
    </form>

     <div class="row">
         <div class="col-sm-9">

         </div>
         <div class="col-sm-3">
             <a href="/participante/crear-participante" style="text-decoration: none">
                 <button class=" btn btn-outline-success btn-block">
                     Crear participante
                 </button>
             </a>
         </div>
     </div>

    <h2>Lista de Participantes</h2>
    <p>Listado de los participantes, actualice o elimine participantes</p>

    <table class="table table-hover animated slideInLeft" >
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Direccion</th>
                <th>Telefono</th>
                <th>Edad</th>
                <th>Talla</th>
                <th>Acciones</th>
            </tr>
        </thead>

        <tbody>
        <% arreglo.forEach( (participante)=> { %>
            <tr>
                <td><%= participante.id %></td>
                <td><%= participante.nombre %></td>
                <td><%= participante.direccion %></td>
                <td><%= participante.telefono %></td>
                <td><%= participante.edad %></td>
                <td><%= participante.talla %></td>
                <td>
                    <form action="/participante/eliminar/<%= participante.id %>" method="POST">
                        <button class="btn btn-outline-danger btn-block" type="submit"
                                style="margin-top: 10px; margin-bottom: 10px">
                            Eliminar
                        </button>
                    </form>
                    <a href="/participante/actualizar-participante/<%= participante.id%>"
                       style="text-decoration: none">
                        <button class="btn-block btn btn-outline-info"
                                style="margin-top: 10px; margin-bottom: 10px">
                            Actualizar
                        </button>
                    </a>
                </td>
            </tr>
        <% }) %>
        </tbody>
    </table>

    <script src="/js/jquery.js"></script>
    <script src="/js/popper.js"></script>
    <script src="/bootstrap/js/bootstrap.js"></script>
    </div>
</body>
</html>